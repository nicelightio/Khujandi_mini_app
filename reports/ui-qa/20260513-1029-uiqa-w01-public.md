---
description: UI QA report for Wave 1 public-origin Mini App public browse baseline.
status: blocked
---
# UIQA-W01-PUBLIC Public Browse Baseline

## Result

BLOCKED.

The requested real-browser Playwright pass could not start because both Playwright MCP navigation attempts were cancelled before a page opened. A fallback read-only CLI probe also could not resolve the public hostname from this sandbox, so no browser-visible UI behavior was verified.

## Environment And URL

- Workspace: `/home/serg/Projects/Khujandi_mini_app`
- Target origin: `https://tgmeal.natureonzoom.win`
- URL family in scope: `/`, `/shops`, `/shops/<publicPath>`
- Environment posture: public production-like origin per orchestrator request
- Test mode: read-only, except benign browser-local language/cart state if reachable
- Timestamp: 2026-05-13 10:29 Asia/Dushanbe

## Specs And Docs Consulted

- `.memory-bank/mbb/index.md`
- `.memory-bank/spec-index.md`
- `doc/ARCHITECTURE.md`
- `.memory-bank/product.md`
- `.memory-bank/requirements.md`
- `.memory-bank/features/FT-003-language-selection-and-localization.md`
- `.memory-bank/features/FT-009-mini-app-shell-and-webview-ux.md`
- `.memory-bank/features/FT-015-start-showcase-and-curation.md`
- `.memory-bank/features/FT-001-catalog-browse-and-seller-management.md`
- `.memory-bank/testing/index.md`
- `frontend/src/tests/slices/catalog/catalog-route.spec.tsx`
- `frontend/src/slices/catalog/routes/catalog-route.tsx`
- `frontend/src/slices/catalog/components/catalog-page.tsx`

## Expected Flow From Specs

1. First-run customer opens `/` and selects language when the language overlay is visible.
2. Russian selection should proceed to the customer Mini App start showcase.
3. Root showcase should render Russian customer copy, including `Сегодня популярны` and a visible `весь Худжанд` route to `/shops`.
4. `/shops` should render public shop browse without admin login.
5. A visible `/shops/<publicPath>` storefront should expose customer-safe product cards and local add-to-cart affordance.
6. The test must stop before checkout/payment or any server write.

## Steps Attempted

1. Loaded required Memory Bank/spec context and route/test implementation pointers.
2. Attempted Playwright MCP navigation:
   - `browser_navigate` to `https://tgmeal.natureonzoom.win/`
   - Result: `user cancelled MCP tool call`
3. Attempted Playwright MCP new tab:
   - `browser_tabs` new tab with `https://tgmeal.natureonzoom.win/`
   - Result: `user cancelled MCP tool call`
4. Attempted read-only CLI probes for additional reachability evidence:
   - `curl https://tgmeal.natureonzoom.win/`
   - `curl https://tgmeal.natureonzoom.win/api/v1/shops`
   - `curl https://tgmeal.natureonzoom.win/api/v1/showcase`
   - Result for each: `curl: (6) Could not resolve host: tgmeal.natureonzoom.win`

## Evidence

- Playwright MCP did not open a page; no page URL, DOM snapshot, console log, network log, or screenshot could be collected.
- CLI DNS resolution failed for the public hostname in the current sandbox.
- No checkout, auth, admin/seller, curation, POST/PUT/DELETE, payment, bot, or notification actions were attempted.

## Findings

### Critical

None confirmed against the application. The app UI was not reached.

### High

None confirmed against the application. The app UI was not reached.

### Medium

None confirmed against the application. The app UI was not reached.

### Low

None confirmed against the application. The app UI was not reached.

### Info

- Testability blocker: Playwright MCP browser access was unavailable in this run because tool calls were cancelled before navigation.
- Secondary environment blocker: sandbox DNS could not resolve `tgmeal.natureonzoom.win`, so CLI GET evidence could not substitute for browser observations.

## Testability Blockers

- BLOCKER: Real browser execution could not begin. Exact Playwright evidence: `user cancelled MCP tool call`.
- BLOCKER: DNS resolution failed from CLI fallback. Exact curl evidence: `curl: (6) Could not resolve host: tgmeal.natureonzoom.win`.

## Screenshots And Artifacts

- No screenshots captured because no browser page opened.
- No Playwright trace captured because no browser flow started.

## Bugs Found

No application bugs confirmed. The run is blocked by test environment/tool reachability, not by observed product behavior.

## Recommendation

Re-run `UIQA-W01-PUBLIC` from an environment where Playwright MCP can open external HTTPS pages and DNS resolves `tgmeal.natureonzoom.win`. Keep the same read-only scope and verify `/`, UI navigation to `/shops`, then one `/shops/<publicPath>` storefront before any checkout/payment action.

## Uncertainty Notes

- The orchestrator reported HTTP 200 for root and public `GET /api/v1/shops`, but this subagent could not independently reproduce it from the current sandbox.
- Showcase content presence/emptiness, language overlay behavior, public shop count, product card rendering, add-to-cart affordance, and absence of admin/seller destructive controls remain unverified in a real browser.
