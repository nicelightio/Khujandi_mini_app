# UI QA Smoke Report: URL Open Check

Date: 2026-05-13 11:12 Asia/Dushanbe
Role: SUBAGENT / tester (`ui_qa`)

## Requested Flow

Smoke test after ui_qa launcher fix.

Scope from orchestrator:
- Target URL: `http://127.0.0.1:5173`
- Confirm whether Playwright can open the URL.
- Record visible page title/text or the exact blocker.
- Do not inspect full specs.
- Do not click checkout/admin/write actions.

## Environment

- Project workspace: `/home/serg/Projects/Khujandi_mini_app`
- Target URL: `http://127.0.0.1:5173`
- Environment inferred: local dev URL
- Browser tool: Playwright MCP

## Specs/Docs Consulted

Full specs were not inspected per orchestrator request.

Minimal workspace check only:
- confirmed working directory
- listed high-level project files and existing `reports/ui-qa/` artifacts

## Steps Performed

1. Confirmed workspace path: `/home/serg/Projects/Khujandi_mini_app`.
2. Confirmed target URL from orchestrator: `http://127.0.0.1:5173`.
3. Attempted to navigate to the target URL with Playwright MCP.

## Findings

### Critical

None.

### High

None.

### Medium

#### Playwright MCP cannot launch configured Chrome

Reproduction:
1. Invoke Playwright MCP navigation to `http://127.0.0.1:5173`.

Expected behavior:
- Browser launches.
- Page at `http://127.0.0.1:5173` opens.
- Visible title/text can be recorded.

Actual behavior:
- Browser creation failed before any page load.
- Exact blocker:

```text
Error: async createBrowserWithInfo: Chromium distribution 'chrome' is not found at /opt/google/chrome/chrome
Run "npx playwright install chrome"
```

Impact:
- The requested browser smoke test could not verify the app page.
- Visible page title/text could not be recorded through Playwright.

Recommended fix:
- Install the Chrome distribution expected by the Playwright MCP environment, or adjust the ui_qa launcher/browser configuration to use an available Playwright browser.
- Re-run the same smoke check after the browser dependency is available.

### Low

None.

### Info

- No checkout, admin, or write actions were attempted.
- No screenshots or traces were created because browser launch failed before navigation.

## Browser Artifacts

None.

## Coverage Gaps / Uncertainty

- App availability at `http://127.0.0.1:5173` was not verified because the browser did not launch.
- Page title, visible content, console errors, and network behavior remain untested.
