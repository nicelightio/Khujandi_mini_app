---
description: План выполнения TASK-FT009-07.
status: active
---
# TASK-FT009-07 Plan

1. Inspect current `PageShell`, checkout page, and focused tests.
2. Extend `PageShell` with a reusable shell-owned `bottomAction` slot while preserving existing metadata-driven action feedback.
3. Add keyboard-safe footer CSS in `webview-shell.css`.
4. Migrate checkout primary CTA to the new shell-owned bottom action zone.
5. Add focused tests for the primitive and checkout smoke.
6. Run targeted tests and sync Memory Bank/docs if implementation lands.
