# TASK-FT004-06 Plan

1. Inspect the admin assignment scaffold and existing frontend API patterns.
2. Add a minimal admin assignment API that calls the backend command path and parses the project error contract.
3. Wire `AdminAssignmentRoute` to the API by default while keeping prop injection for tests.
4. Add a duplicate-submit guard that does not rely only on async React state.
5. Update admin frontend tests for loading, success, controlled error rendering, and duplicate-submit prevention.
6. Run focused verification and sync backlog/Memory Bank/progress/report artifacts.
