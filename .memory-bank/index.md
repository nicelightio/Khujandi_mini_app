---
description: Главная карта знаний проекта (table of contents) для агентов.
status: active
---
# Memory Bank Index

## Навигация

- [.memory-bank/mbb/index.md](mbb/index.md): Правила ведения Memory Bank (MBB).
- [.memory-bank/product.md](product.md): Продукт (C4 L1).
- [.memory-bank/requirements.md](requirements.md): Требования + RTM.
- [.memory-bank/epics/index.md](epics/index.md): Эпики MVP (C4 L2) и их scope.
- [.memory-bank/features/index.md](features/index.md): Feature-спеки MVP (C4 L3) для `/prd-to-tasks`.
- [.memory-bank/tasks/backlog.md](tasks/backlog.md): Backlog / waves.
- [.memory-bank/tasks/plans/index.md](tasks/plans/index.md): Роутер по implementation plans и bugfix plans (C4 L4).
- [.memory-bank/tasks/archive/index.md](tasks/archive/index.md): Архив historical task cards и backlog waves.

- [.memory-bank/spec-index.md](spec-index.md): Реестр normative docs и маршрутизация по source-of-truth.
- [.memory-bank/glossary.md](glossary.md): Общий словарь терминов и доменных значений.
- [.memory-bank/invariants.md](invariants.md): Глобальные MUST/NEVER правила.
- [.memory-bank/architecture/index.md](architecture/index.md): Duo + boundaries (WHAT/WHY).
- [.memory-bank/diagrams/index.md](diagrams/index.md): ASCII-схемы для быстрого входа в контекст по specs, коду и runtime.
- [.memory-bank/guides/index.md](guides/index.md): Valid HOW docs для использования, запуска и troubleshooting.
- [.memory-bank/adrs/index.md](adrs/index.md): ADR решения.

- [.memory-bank/contracts/index.md](contracts/index.md): Контракты и boundary specs (prefer when present).
- [.memory-bank/states/index.md](states/index.md): Lifecycle/state rules (prefer when present).
- [.memory-bank/runbooks/index.md](runbooks/index.md): Runbooks и operational procedures.
- [.memory-bank/testing/index.md](testing/index.md): Testing strategy.
- [.memory-bank/workflows/index.md](workflows/index.md): Execution/workflow docs для task loop и MB sync.
- [.memory-bank/changelog/archive/index.md](changelog/archive/index.md): Архив старых записей changelog.
- [.memory-bank/skills/index.md](skills/index.md): Skill registry.
- [.memory-bank/bugs/index.md](bugs/index.md): Bug records и verification failures.

## Recent updates

- `TASK-FT017-04`: verified `PASS`; final repo-local guarded e2e mock payment closure passed backend checkout-payment Jest, frontend checkout-payment Jest, frontend build, lint and `git diff --check`. `FT-017` is terminal for the scoped `success/paid` baseline; mock failed/timeout/pending and real production provider design remain out of scope.
- `TASK-FT017-03`: verified `PASS`; checkout-only debug/e2e affordance depends on backend `mockPaymentAvailable` metadata from the guarded checkout runtime, renders only in ready checkout with composition, keeps the existing submit button as the only action, and preserves backend-only payment trust.
- `TASK-FT017-01`: verified `PASS`; guarded backend mock provider boundary for `checkout-payment` is disabled by default, explicit server-side `PAYMENT_PROVIDER=mock` selects `mock` only outside `NODE_ENV=production`, production-like startup rejects mock, and `DEBUG=true` alone refuses checkout without creating an order. No checkout UI affordance, catalog/cart change, shared abstraction or externally selectable failed/timeout/pending mock outcome was added.
- `FT-017`: docs/protocol decomposition added for guarded repo-local/e2e mock payment mode in `checkout-payment`: future implementation must replace/gate the old implicit local mock through server-side `PAYMENT_PROVIDER=mock`, enforce `NODE_ENV !== "production"` baseline guard, reject/refuse mock in production, support first-baseline `success/paid` only, and keep frontend `DEBUG=true` as checkout-only presentation affordance rather than payment trust.
- `mock payment spec layer`: `checkout-payment` now has a KISS normative shape for repo-local/e2e mock payment: server-side `PAYMENT_PROVIDER=mock` plus non-production guard is the only trust gate, `DEBUG=true` is UI-only, first baseline requires success mode only, failed/timeout are follow-ups, and `.memory-bank/runbooks/e2e-mock-payment.md` captures future verification targets and forbidden cases.
- `TASK-FT016-16`: implementation aligned the mini-app/admin-web polling consumers with v2 delivery lifecycle. Customer order tracking now exposes only courier-owned actions through `PICKED_UP -> IN_PROGRESS -> DELIVERED`, consumes operator/admin `COMPLETED` via polling, keeps terminal states closed, and preserves string-only opaque cursor handling. Focused order-tracking/admin tests, frontend build and `git diff --check` pass; verifier closure remains separate.
- `TASK-FT016-15-FIX`: implementation repaired the mounted operator/admin status command role boundary by normalizing admin-access `manager` to delivery-tracking `operator` only for that command. Runtime coverage proves authenticated `MANAGER` can close `DELIVERED -> COMPLETED`, invalid manager `PICKED_UP -> COMPLETED` still returns `409`, and `BOSS` remains rejected without status side effects. Focused delivery-tracking tests and `git diff --check` pass; verifier closure remains separate.
- `TASK-FT016-15`: implementation added operator/admin status control for allowed next transitions only, including `DELIVERED -> COMPLETED`, with admin-web confirmation before command execution. Courier completion remains rejected through the courier tracking/bot path, invalid broad overrides return `409` without side effects, and runtime operator history/read model surfaces actor role/name. Focused delivery-tracking tests, focused admin assignment tests, frontend build and `git diff --check` pass; verifier closure remains separate.
- `TASK-FT016-14`: implementation enabled the `delivery-tracking` v2 courier lifecycle `ASSIGNED -> PICKED_UP -> IN_PROGRESS -> DELIVERED` and removed courier completion from bot tracking actions. Legacy active orders already in `IN_PROGRESS` can still move to `DELIVERED`; skip/regression/replay and courier `DELIVERED -> COMPLETED` attempts return `409` without persistence side effects. Focused delivery-tracking unit/integration/runtime tests and `git diff --check` pass; verifier closure remains separate.
- `TASK-FT016-13-FIX`: implementation repaired the customer order-tracking parser for the real timeout-produced `order.delayed` event shape. Polling now maps `newStatus`/`oldStatus` into canonical customer status fields, and focused parser/open-route tests prove an already-open read-only tracking screen renders `DELAYED` waiting/problem copy without mutation controls. Focused order-tracking Jest and `git diff --check` pass; verifier closure remains separate.
- `TASK-FT016-13`: implementation surfaced `DELAYED` in presentation/read-copy only. Admin operator read-model rows and top alert now treat `status=DELAYED` as danger/delayed copy even if severity is stale, while customer order tracking renders `DELAYED` as waiting/problem copy with no courier progress controls. Focused admin assignment Jest, `npm run test:order-tracking:frontend -- --runInBand`, `git diff --check`, and changed markdown link validation pass; verifier closure remains separate.
- `TASK-FT016-12`: implementation added the explicit KISS offer timeout evaluator for `delivery-assignment`. The command repeats pending offer notifications once after 3 minutes, expires still-pending offers after 6 minutes, sets/keeps unassigned orders `DELAYED`, records timeout/delayed events after persistence, notifies operators through the existing Telegram dispatcher when targets exist, penalizes only manual/personal target couriers once, and leaves claimed/assigned/post-assignment orders untouched. Focused delivery-assignment tests pass; verifier closure remains separate.
- `TASK-FT016-10`: implementation added atomic courier claim for pending assignment offers through the Telegram bot callback harness plus `delivery-assignment` application/repository boundary. Successful claim is the v2 `order.assigned` publish point; duplicate/concurrent/wrong/invalid claims stay controlled and side-effect safe. Focused delivery-assignment tests and `git diff --check` pass; verifier closure remains separate.
- `TASK-FT016-08`: verified `PASS`; the Telegram courier menu harness remains transport-only, callback parsing produces delivery-assignment service intents only, and no direct Prisma bot writes/imports, webhook runtime, admin UI, offers, claims, status progression, timeout or order side effects were added. `TASK-FT016-09` can be prepared next from the FT-016 implementation plan.
- `TASK-FT016-08`: implementation added the Telegram courier availability harness for the `Курьер` menu, start-work, stop-after-5-min and auto-offer ON/OFF buttons, plus callback parsing into delivery-assignment service intents. No webhook runtime, direct Prisma writes, offers, claims, status progression or order side effects were added; `npm run test:delivery-assignment` and `git diff --check` pass.
- `TASK-FT016-07-FIX`: verified `PASS`; the courier availability presentation/controller exposure is removed, the application/domain/infra availability boundary remains intact, and `BUG-2026-05-09-task-ft016-07-presentation-scope-leak` is resolved. `TASK-FT016-08` may be prepared next from the FT-016 plan.
- `TASK-FT016-07-FIX`: implementation removed the out-of-scope courier availability presentation exposure from `backend/src/slices/delivery-assignment/presentation/delivery-assignment.controller.ts` while preserving the `TASK-FT016-07` application/domain/infra availability boundary. Focused delivery-assignment tests, `git diff --check` and changed markdown local link validation pass; backlog closure remains with verifier.
- `TASK-FT016-07`: verification returned `FAIL` despite passing focused checks because the implementation touched the `delivery-assignment` presentation controller while the approved task scope was backend `application/domain/infra/tests` only. `TASK-FT016-07-FIX` is ready to remove/defer that exposure before later FT-016 tasks proceed.
- `TASK-FT016-07`: implementation added the delivery-assignment backend application boundary for courier availability. Couriers can start work, set a stop-after-5-min cutoff, toggle auto-offer participation and query active/free state; free is server-calculated from `ASSIGNED`, `PICKED_UP`, `IN_PROGRESS`, and `DELIVERED` orders, while offer creation, claim, bot/menu runtime, fan-out, timeout and order side effects remain out of scope.
- `TASK-FT016-06`: verified `PASS`; the admin-web operator delivery surface renders inert action cells for targeted offer, status control confirmation and bot chat redirect with explicit backend/runtime-not-enabled labels, while keeping the read surface, protected admin shell and no-mutation boundary intact.
- `TASK-FT016-05`: verified `PASS`; the admin-web read-side operator attention alert and local deterministic sorting now sit over the existing operator delivery read model. `DELAYED`/no-courier orders surface in the top alert, severity tones are deterministic, `DELAYED` rows blink red, and sort controls cover urgency, created time, status, courier absent/name, assigned time and last message availability without adding backend mutations or timeout/bot/offer behavior.
- `TASK-FT016-04`: verified `PASS`; existing protected admin assignment route is now a read-only operator delivery orders surface over `GET /api/v1/admin/operator/delivery/orders`, with the 4-day backend window, severity chips, courier absent/current markers, latest-message placeholders and expandable status history; old direct assignment is no longer the default CTA.
- `TASK-FT016-03`: verified `PASS`; backend dev runtime now exposes a read-only admin-protected operator delivery read endpoint for today plus previous 3 local calendar days, with status/courier/severity/history/message-placeholder fields and regression coverage for existing assignment/cancellation/refund command routes.
- `FT-016/operator delivery ops`: spec layer now targets a KISS desktop-first operator panel with top delayed/unassigned alert, `operator` role (admin includes operator), courier bot availability menu, optional auto-offer to active free couriers, atomic first-claim-wins assignment, `DELAYED`, `PICKED_UP`, and operator-owned `DELIVERED -> COMPLETED`.
- `created-shops design-flow`: created/provisioned shop UX now has `.design/created-shops/` brief, IA, scoped token notes and task checklist; `/admin/catalog/shops/provision` was refined as a presentation-only provisioning workspace with clearer skeleton/binding/visibility context and scan-friendly public-path cards, while contracts/RTM/domain semantics remain unchanged.
- `FT-015 repair`: post-review KISS bugfix closed the admin curation usability/preflight blockers without expanding architecture: storefront long-press/context-menu now keeps a stable add-to-showcase action, curation writes show controlled feedback and refresh/reconcile state, storefront admin menu can favorite/unfavorite the current shop, and `OPTIONS` preflight includes `DELETE` for showcase curation endpoints; atomic favorite cap and curation audit/event enrichment remain explicit residual debt rather than part of this repair.
- `FT-015`: repo-local стартовая Витрина реализована и verified: после выбора языка root `/` показывает "Сегодня популярны", до 3 избранных магазинов и ссылку "весь Худжанд" на `/shops`; backend хранит только live catalog references, public read скрывает `NOT_WORKING`/deleted refs, а curation endpoints доступны только platform admin `BOSS`/`ADMIN`.
- `catalog/customer product-card interactions`: customer-facing `/shops/:publicPath` product cards now use short tap/click as the add-to-cart gesture, render an in-card quantity counter with +/- controls after selection, and reserve long press for a temporary description tooltip above the touch point that disappears on release; seller edit mode still owns product click/context-menu editing separately.
- `catalog/storefront product-card simplification`: customer-facing `/shops/:publicPath` now removes visual wrapper frames around menu/legacy/order-draft surfaces in browse mode and leaves the storefront as product cards only; every product card has a media slot, name and price, while seller edit mode keeps its existing menu/tab/editor affordances.
- `Ops/Prisma production baseline`: Prisma rollout drift found during TgMeal prod DB bootstrap is repaired in source: `Shop.orders` restores schema validation and `backend/prisma/migrations/20260401000000_init_current_schema_baseline` bootstraps blank PostgreSQL databases before the later ReviewDraft and shop-identity incremental migrations; the deploy runbook now requires disposable empty-DB `prisma migrate deploy` verification for DB rollout changes.
- `Ops/GitHub-only deploy policy`: production deploys must never build/copy directly from the active development source folder; release flow is branch -> GitHub PR -> review/CI -> merge/push to GitHub -> GitHub Actions SSH-runs server deploy, and `tgmeal-deploy` pulls `origin/main` into `/srv/tgmeal/app` while refusing dirty local deploy checkouts or non-GitHub remotes.
- `Ops/AlmaLinux deploy`: deployment docs and compose baseline now target the current AlmaLinux prod host with existing Traefik on `80/443`, external Docker network `web`, no host nginx, a checked-in `deploy/scripts/tgmeal-deploy-alma.sh` template, and explicit PhotoChanger/Traefik safety invariants.
- `FT-009/frontend shell`: initial screen loads now show one pre-React lightweight inline SVG blue-spruce splash with Telegram-blue tinted background for about 3.9 seconds; React dismisses that initial overlay instead of remounting a second splash, preserving CSS draw/fade motion without dependencies or slice behavior changes.
- `catalog/admin-dashboard`: public Mini App home now exposes only the main deployed admin link `https://tgmeal.natureonzoom.win/admin`; the protected admin dashboard owns the links to assignment, cancellation/refund, catalog provisioning, order/courier tracking and narrow seller store-status surfaces, while storefront pages keep the customer-safe `Вернуться` link back to the catalog.
- `Refactor hardening wave`: large-file refactors split the repo-local `dev-runtime` server and the `reviews-feedback` Prisma repository into focused local modules, then closed high-priority review findings for review duplicate cursors/draft CAS, order-cancellation atomic cancellation, admin auth HTTP hardening, seller auth error contract, and dev-runtime body/cookie parsing. Remaining hardening items are tracked in `tasks/backlog.md` as ready follow-ups rather than changing RTM lifecycle.
- `TASK-FT014-07`: repo-local customer events repair is complete: checked-in `dev-api-server` now mounts authenticated `GET /api/v1/events?since=<cursor>`, filters events to the current Mini App customer's orders, returns checkout success `revision` from the event-stream cursor instead of `order.id`, and keeps opaque cursor inputs stable; this evidence supports `REQ-033` repo-local verification, while Android Telegram smoke remains advisory pre-release risk evidence.
- `TASK-MB-REVIEW`: prior review blockers were resolved or reclassified: repo-local `/api/v1/events` and cursor compatibility were repaired in `TASK-FT014-07`, and missing Android Telegram evidence is now advisory pre-release risk rather than blocker.
- `TASK-FT014-05`: customer order status polling now resumes through existing shell lifecycle state without raw Telegram subscriptions, clears stale in-flight polling on deactivation, and ignores duplicate/out-of-order lifecycle regressions or post-terminal progress events while preserving opaque cursor progress; final repo-local closure is now done through `TASK-FT014-06`.
- `TASK-FT014-04`: customer order status now renders customer-safe lifecycle copy for paid/waiting, assigned, courier progress, delivered, completed and cancelled terminal states from the existing `order-tracking` mini-app surface, while read-only sessions expose no courier/admin buttons, audit details or refund internals; `TASK-FT014-05` is ready for resume/terminal-state hardening.
- `TASK-FT014-03`: customer order status now polls the existing `GET /events?since=<opaque>` contract from the `order-tracking` frontend surface, accepts contract/runtime cursor field variants as strings only, keeps empty windows and duplicate revisions stable, and leaves lifecycle commands outside the `mini-app` customer contour; `TASK-FT014-04` is ready for customer-safe lifecycle rendering.
- `TASK-FT014-02`: checkout success now links to customer order status from real paid-order metadata (`orderId` + string `revision`), `/tracking?orderId=...&cursor=...` starts read-only at `CREATED`, missing/lost identity recovers to catalog instead of fake tracking data, and `TASK-FT014-03` is ready for polling consumer wiring.
- `TASK-FT013-07`: final repo-local checkout-payment gates passed for mounted composition-backed checkout, paid `CREATED` order persistence, retry/no-order paths and idempotency; after the evidence policy update, `REQ-032` is `verified` and `TASK-FT013-08` is only advisory pre-release smoke.
- `TASK-FT013-06`: mounted checkout failure paths are now retry/repair safe; failed, canceled, timeout and ambiguous provider outcomes create no order, malformed/stale composition returns controlled repair metadata, and duplicate trusted payment confirmation reuses the existing paid order without creating a second order.
- `TASK-FT013-05`: mounted checkout submit now persists a paid `CREATED` order from the authenticated Mini App session plus server-revalidated `FT-012` composition and provider-trusted local `PAID` confirmation, returning `orderId`, `updated_at` and string `revision` metadata for downstream customer status visibility.
- `TASK-FT013-04`: checked-in checkout frontend API now uses mounted Mini App runtime endpoints for Telegram auth, language sync and checkout submit instead of local stub success responses; the dev runtime requires the real HttpOnly Mini App session and returns controlled no-order payment-confirmation-required semantics until `TASK-FT013-05` owns paid `CREATED` persistence.
- `TASK-FT013-03`: backend `checkout-payment` now has an explicit catalog composition revalidation seam before payment/order persistence; valid drafts are checked against current shop/product/quantity/price/currency facts, stale drafts return controlled repair responses, and the next executable `FT-013` task is mounted Mini App auth/payment runtime wiring.
- `TASK-FT013-02`: checkout route entry now requires the `FT-012` composition handoff draft; valid drafts render selected shop, line items, quantities, display snapshots and preview totals for customer confirmation, while direct or invalid `/checkout` access recovers to catalog/cart without fake order data or payment/order side effects.
- `TASK-FT013-01`: docs-first checkout handoff boundary is frozen; `catalog` produces the `FT-012` composition draft, `checkout-payment` consumes/revalidates/pays/creates the order through the existing `FT-002` auth/payment boundary, and `TASK-FT013-02` is ready for composition-backed checkout route entry.
- `TASK-FT012-06`: final `FT-012` verification is complete; public storefront composition now blocks checkout when a selected same-shop product becomes unavailable, shows controlled repair feedback, and `REQ-031` is verified for visible single-shop, side-effect-free checkout-ready composition.
- `TASK-FT012-05`: public storefront checkout CTA now produces the `customer-order-composition-contract.md` handoff payload from the catalog-owned cart draft, blocks empty/invalid composition, and stores only non-sensitive route handoff data without starting payment, creating orders, reserving stock or publishing lifecycle events.
- `TASK-FT012-04`: public storefront cart selection now enforces explicit single-shop replacement/clear behavior in the `catalog` mini-app UI; cross-shop add attempts keep the existing cart unchanged until the customer chooses `Replace cart` or `Clear cart`, and focused coverage proves mixed-shop draft payloads cannot be produced.
- `TASK-FT012-03`: public storefront customer UI now wires `catalog` composition add/update/remove actions into existing storefront product cards and shows an order draft summary with selected shop, line items, display snapshots, preview totals and checkout readiness without adding checkout/payment/order side effects or seller delete semantics.
- `TASK-FT012-02`: `catalog` now has slice-local customer composition state plus a contract payload mapper for public storefront products, with focused tests covering empty state, add/merge/update/remove, same-shop blocking and `shop_public_path` handoff shape without order/payment side effects.
- `TASK-FT012-01`: `FT-012` customer composition boundary is frozen docs-first; `catalog` owns the single-shop composition producer, `checkout-payment` owns downstream revalidation/payment/order creation, and the handoff remains a contract artifact rather than a shared cart business module.
- `FT-014`: implementation plan, protocol docs and execution-ready backlog decomposition are added for customer-facing order status visibility after paid order creation, using `FT-005` ordered polling/events and customer-safe lifecycle states without moving delivery operations into the `mini-app` customer contour.
- `FT-013`: implementation plan, protocol docs and execution-ready backlog decomposition are added for customer checkout handoff from `FT-012` composition, mounted Mini App auth/payment runtime, server-side catalog revalidation, paid-only `CREATED` order creation and final Telegram-sensitive verification.
- `FT-012`: implementation plan, protocol docs and execution-ready backlog decomposition are added for customer product selection, single-shop cart/order composition, explicit replace/clear behavior, checkout handoff payload production, and final side-effect-free verification.
- `EP-001/FT-012/FT-013/FT-014`: customer ordering workflow is now specified end-to-end as `catalog selection -> cart/order composition -> checkout handoff -> trusted payment/order CREATED -> customer status visibility`, with new `REQ-031/032/033` and a cross-slice composition payload contract that links `catalog` to `checkout-payment` without duplicating `FT-002` payment ownership or `FT-005` delivery tracking ownership.
- `FT-010/FT-009/admin-web`: added a dedicated frontend-polish pass after the public storefront bugfix wave, keeping the existing storefront/admin trees and cheap runtime profile while layering in magnetic nav/buttons, subtle status-chip pulse, viewport-tied beam drift, mild storefront hero parallax, media crossfade on storefront image updates, animated active tab underline, and stronger product image hover/press zoom without introducing new heavy motion libraries or changing debug/storefront runtime flows.
- `FT-010/FT-011`: mounted `catalog` runtime now honors canonical public storefront product field selection for non-owner `/shops/:publicPath` reads, so public payloads again include nullable browse-safe `description`/`imageUrl` fields instead of dropping them inside the in-memory Prisma adapter and tripping the shared storefront frontend parser.
- `FT-010/FT-011`: shared storefront/frontend catalog API now fails closed when browse or storefront payloads omit canonical `publicPath`, so alias-aware `/shops/:publicPath` resolution can no longer silently degrade back to technical `shop.id` fallback in the client parser while default hero/background visuals remain presentation-only placeholders rather than route/data reconstruction.
- `FT-010/FT-011`: shared storefront frontend phase-2 now consumes the canonical public storefront payload on non-owner `/shops/:publicPath` reads instead of reconstructing browse mode from `GET /shops` + `GET /shops/:ref/products`; owner load order still prefers protected seller storefront data, controlled not-found/error states remain intact, and focused frontend catalog Jest coverage now proves non-owner access to canonical media/menu/legacy tabs.
- `FT-010/FT-011`: phase-1 shared storefront public runtime now has a canonical `GET /api/v1/shops/:publicPath` read model that resolves both immutable public-path aliases onto the same persisted storefront record and returns public-safe `description`/hero-background media/menu-page-product data instead of relying on synthetic `shops + products` fallback reconstruction.
- `FT-010`: shared storefront now has an explicit env-gated `DEBUG=TRUE` diagnostic mode: mounted `/api/v1/seller/*` runtime can temporarily bypass owner-only guard through the same canonical catalog path, storefront UI can expose copyable debug logs for `load -> edit -> save -> reload`, and `scripts/dev-api.ts` passes the same flag into backend debug logging for seller storefront read/write traces.
- `admin-web`: protected assignment/cancellation/provisioning API calls now perform one shared admin refresh-and-retry on `AUTH_REQUIRED`, so a boss/admin shell that stays open past the 15-minute access-cookie TTL can still recover through the valid refresh-cookie boundary instead of falsely failing provisioning until manual reload.
- `FT-010`: shared `/shops/:publicPath` storefront now uses a mobile-first poster composition with a 38/62 hero-to-content split, sticky horizontal menu tabs, and seller-side client crop flows for shop header/product imagery via `react-easy-crop`, while still saving through the existing catalog string-image boundary.
- `FT-011`: mounted catalog runtime now repairs legacy persisted shop rows that lacked immutable public-path fields, so deployed `/api/v1/admin/catalog/shops` rehydrates DB-backed shops onto the canonical flat summary with `primaryPublicPath` and `secondaryPublicPath` instead of leaking pre-path persisted shape.
- `admin-web`: `/admin/login` now uses a dedicated login-first hero composition, while `assignment`, `cancellation`, and `catalog provisioning` reuse one lightweight operator-surface grammar (fact lists, selection rows, table-style shop list) on top of the shared cinematic dark shell, keeping future AI edits concentrated in existing page files plus `frontend/src/admin/styles/admin-theme.css`.
- `FT-010/FT-011`: storefront routing is now explicitly split from technical shop identity: `/shops/:publicPath` resolves immutable persisted public paths, browse links default to the human-readable vanity path, and `shop.id` remains an internal technical identifier while provisioning conflicts still key on `sellerId + shop name`.
- `admin-web`: added a shared dark cinematic visual shell in `frontend/src/admin/styles/admin-theme.css` plus route-aware topbar/navigation, so admin pages now use one lightweight CSS-driven scene language (soft light spots, beam drift, restrained motion) instead of page-local plain layout.
- `FT-010/FT-011`: `/admin/catalog/shops/provision` now has a narrow catalog-owned admin read model for existing provisioned shops, so the page reloads canonical persisted shop summaries (including `NOT_WORKING`) instead of relying only on route-local post-submit state.
- `FT-009`: `TASK-FT009-08` is complete with `/verify` `PASS`, but post-change `red-verify` returned `semantic-concern`: shell-owned capability/degradation policy is now centralized, yet the current reduced-runtime fallback also drops the keyboard-safe bottom-action layout to `inline`, so final risk closure remains with `TASK-FT009-09`.
- `FT-009`: `TASK-FT009-07` is complete; `PageShell` now exposes a shared shell-owned sticky bottom action zone and checkout routes its primary CTA through that footer path instead of page-local action placement, but post-change `red-verify` returned `semantic-concern` because keyboard-open reachability still lacks explicit Telegram runtime evidence and the new page-level scroll model has only narrow checkout-focused validation.
- `FT-011`: identical provisioning now consistently fails on the canonical `sellerId + shop name` key even when `telegramId` differs, because the stale service-level Telegram-binding precheck was removed and focused unit/integration/runtime coverage now exercises that exact duplicate path.
- `FT-011`: `TASK-FT011-09` is complete; the mounted repo-local provisioning path no longer enforces accidental `single-shop-per-seller` behavior, and focused integration/runtime coverage now proves one seller identity may own multiple admin-provisioned shops when canonical shop names differ.
- `FT-011`: `TASK-FT011-06` is complete; final catalog gates plus manual restart-smoke evidence now verify the DB-backed runtime baseline end to end, so `REQ-027/028` are promoted to `verified` and `FT-011` is closed.
- `FT-011`: `TASK-FT011-05` is complete; mounted runtime durability coverage now proves persisted provisioning still fails closed through the controlled conflict contract after restart on the same DB path, and `npm run test:catalog:runtime` exposes that DB-backed regression suite as an explicit gate.
- `FT-011`: `TASK-FT011-04` is complete; mounted seller capability checks and seller storefront payload resolution now use repository-backed persisted catalog reads instead of direct `catalogState` access, and runtime coverage proves seller storefront data survives restart on the same DB path.
- `FT-011`: `TASK-FT011-08` is complete; seller rename flows are now reconciled with the durable `sellerId + shop name` invariant, so mounted/runtime rename collisions fail through controlled `SHOP_RENAME_CONFLICT` `409` semantics instead of leaking raw persistence behavior.
- `FT-011`: the `semantic-concern` opened by post-change `red-verify` on `TASK-FT011-07` is now closed by `TASK-FT011-08`, so the durable `sellerId + shop name` invariant no longer leaves seller rename collisions on a raw persistence-failure path.
- `FT-011`: `TASK-FT011-07` is complete; catalog provisioning conflicts are now enforced at the persistence boundary through durable `sellerId + shop name` uniqueness, and hostile integration/runtime coverage proves repeated or concurrent identical provisioning leaves exactly one starter bundle.
- `FT-011`: `TASK-FT011-03` is complete; admin provisioning now rejects repeated identical `sellerId + telegramId + shop name` requests before repository writes, while integration coverage still proves the starter `shop + binding + menu pages + products` bundle commits or rolls back atomically.
- `FT-011`: `TASK-FT011-02` is complete; hidden `catalog` demo bootstrap no longer comes from in-file process memory, because the mounted repo-local runtime now seeds from `backend/prisma/seeds/catalog-runtime-baseline.json` and reuses a SQLite-backed persisted catalog state across restart when the same DB path is used.
- `FT-011`: `TASK-FT011-01` is complete; the mounted repo-local `catalog` runtime now boots through the Prisma-backed module instead of defaulting to `InMemoryCatalogRepository`, while durable seed/bootstrap and restart-safe closure remain with later `FT-011` tasks.
- `FT-011`: implementation plan, protocol docs and execution-ready backlog decomposition are added for DB-backed `catalog` runtime baseline, transactional provisioning and restart-safe storefront resolution.
- `FT-011`: added `.memory-bank/features/FT-011-db-backed-catalog-runtime-baseline.md` plus `REQ-027/028` to re-baseline `catalog` around durable DB-backed runtime, transactional provisioning, and restart-safe storefront resolution instead of the current repo-local in-memory runtime wiring.
- Drift correction: `FT-010` remains closed for seller contour behavior, but durable catalog runtime closure is now tracked separately in `FT-011` so current spec-layer no longer overstates the checked-in runtime path.
- `FT-010`: added dedicated runbook `.memory-bank/runbooks/ft-010-manual-verification.md` for manual testing of admin provisioning, shared storefront seller edit mode, seller-web status toggle, controlled `/shops/:publicPath` missing/error states, and no-delete baseline.
 - `FT-010`: `TASK-FT010-21` is complete; `/shops/:publicPath` no longer fabricates a synthetic storefront when canonical seller data and public data are both absent or failing, and now returns controlled not-found/error states while preserving valid public browse flows.
- `FT-010`: post-change `red-verify` for `TASK-FT010-08` returned `semantic-pass`; final feature closure remains substantively aligned for the checked-in repo scope and opened no new follow-up.
- `FT-010`: `TASK-FT010-08` is complete; final repo-local verification/docs sync now explicitly covers shared storefront edit-mode reuse, admin-provisioned skeleton bootstrap, Telegram-linked seller access reuse, `WORKING/NOT_WORKING` owner/public visibility gating, and delete-free baseline evidence, so `REQ-024/025/026` are now done for the checked-in feature scope.
- `FT-010`: post-change `red-verify` for `TASK-FT010-20` returned `semantic-pass`; the narrow seller-web status hardening is substantively aligned for the checked-in mounted runtime scope and opened no new follow-up.
- `FT-010`: `TASK-FT010-20` is complete; narrow `seller-web` status toggles now submit status-only intent and the mounted seller runtime preserves existing shop metadata when those toggles omit storefront fields, closing the stale-overwrite follow-up from `TASK-FT010-07` without widening `seller-web` into a broad editor.
- `FT-010`: `TASK-FT010-07` is complete; `admin-web` provisioning now submits through the mounted protected command with controlled starter-bootstrap feedback, while narrow `/seller/shops/status` now loads owned shops via the shared Telegram-linked seller runtime and persists `WORKING/NOT_WORKING` without adding reporting/stats or a second seller auth model.
- `FT-010`: post-change `red-verify` for `TASK-FT010-19` returned `semantic-pass`; the unpaged legacy-product compatibility path is substantively aligned for the checked-in shared seller storefront/runtime scope and opened no further follow-up.
- `FT-010`: `TASK-FT010-19` is complete; canonical seller storefront reads on `/shops/:publicPath` now preserve legacy owner-visible products without explicit menu-page linkage through an explicit `unpagedProducts` compatibility path instead of rendering older owned shops empty.
- `FT-010`: post-change `red-verify` for `TASK-FT010-18` returned `semantic-concern`; canonical shared-storefront seller wiring now uses the right protected read/write boundary, but older checked-in shops with unpaged products can still render empty owner storefronts, so follow-up `TASK-FT010-19` is now `ready`.
- `FT-010`: `TASK-FT010-18` is complete; shared storefront seller mode on `/shops/:publicPath` now loads canonical owner-visible menu pages/products from the protected seller runtime boundary, submits through real checked-in seller write endpoints, and reloads canonical data instead of using public/synthetic reconstruction plus frontend-local save simulation.
- `FT-010`: post-change `red-verify` for `TASK-FT010-06` returned `semantic-concern`; shared storefront seller edit mode is on the correct tree, but it still uses synthetic/publicly-derived storefront content and frontend-local save simulation instead of canonical seller storefront data wiring, so follow-up `TASK-FT010-18` is now `ready`.
- `FT-010`: `TASK-FT010-06` is complete; `/shops/:publicPath` now keeps seller edit mode on the existing shared `CatalogRoute`/`CatalogPage` tree, supports owner-only contextual activation plus controlled save feedback, and leaves non-seller visitors in browse-only mode without a second storefront implementation.
- `FT-010`: `TASK-FT010-17` is complete; unknown `/admin/*` paths now remain inside the `admin-web` contour but render explicit not-found feedback instead of silently resolving to assignment/login fallbacks.
- `FT-010`: `TASK-FT010-16` is complete; root contour selection now honors slash-bounded `/admin` and `/seller` route families, adjacent hostile prefixes stay on the customer app contour, and unsupported `/seller/*` paths render explicit seller-web not-found feedback instead of silently falling back to the status scaffold.
- `FT-010`: `TASK-FT010-02` is complete; checked-in frontend routing now distinguishes `mini-app`, `admin-web`, and narrow `seller-web` contours, keeps `/shops/:publicPath` on the same `CatalogRoute` tree as public browse, and adds seller/admin page shells for later status-toggle and provisioning wiring without a second storefront bootstrap.
- `FT-010`: post-change `red-verify` for `TASK-FT010-02` returned `semantic-concern`; current contour matching is broader than the normative `/admin/*` and `/seller/*` route-family intent, so follow-up `TASK-FT010-16` is now `ready`.
- `FT-010`: `TASK-FT010-15` is complete; the checked-in non-persistent `catalog` adapter no longer uses a private seller-only sink and now records seller shop/menu/product write events into the shared runtime `events`-store analogue used for parity with the project event model.
- `FT-010`: post-change `red-verify` for `TASK-FT010-15` returned `semantic-pass`; no new bug or follow-up was opened, and the sink-level adapter parity loop from `TASK-FT010-14` is now substantively closed.
- `FT-010`: `TASK-FT010-14` is complete; seller shop/menu/product write observability is now explicit at the `CatalogRepository` boundary and parity-tested across both Prisma-backed and in-memory/runtime `catalog` adapters.
- `FT-010`: `TASK-FT010-13` is complete; seller shop/menu/product writes in `catalog` now emit explicit persisted `catalog.*` events, and the observability baseline for this seller write surface is frozen as event-backed inside the owning slice without a separate catalog audit table.
- `FT-010`: post-change `red-verify` for `TASK-FT010-05` returned `semantic-concern`; seller catalog ownership/no-delete logic is substantively correct, but the expanded seller write surface still lacks explicit event/audit semantics, so follow-up `TASK-FT010-13` is now `ready`.
- `FT-010`: `TASK-FT010-05` is complete; backend `catalog` seller writes now cover owned shop metadata edits plus menu page add/rename flows, and product writes fail closed when a menu page or target shop does not belong to the authenticated seller, while the one-free-rename and no-delete baseline remain intact.
- `FT-010`: `TASK-FT010-11` is complete; seller-protected catalog reads no longer depend on a `dev-runtime`-local Mini App auth/session clone, because repo-local `POST /api/v1/auth/telegram` and `GET /api/v1/seller/shops[/:shopId]` now share the checked-in `checkout-payment` module boundary and one persistent session state.
- `FT-010`: `TASK-FT010-12` is complete; repo-local `POST /api/v1/auth/telegram` now consumes the shared `checkout-payment` cookie transport descriptor directly, so Mini App cookie issuance no longer depends on the `pendingMiniAppSessionToken` route-local side channel.
- `FT-010`: post-change `red-verify` for `TASK-FT010-12` returned `semantic-pass`; no new blocking follow-up was opened beyond the standing caution that raw cookie transport data must stay server-only.
- `FT-010`: the earlier `red-verify` concern from `TASK-FT010-04` is now closed by `TASK-FT010-11`, so seller ownership resolution and seller-protected runtime reads both sit on one checked-in Mini App auth/session boundary.
- `FT-010`: `TASK-FT010-04` is complete; checked-in `catalog` now resolves seller-owned reads from the Telegram Mini App cookie session family plus `SellerShopBinding` instead of trusting client-side seller flags or `shop.sellerId` alone, and protected `/api/v1/seller/shops` owner reads keep `NOT_WORKING` shops visible only to the owning seller while public browse stays auth-free.
- `FT-010`: `TASK-FT010-10` replaced the provisioning route's refresh-cookie shortcut with a reusable protected admin runtime boundary, so privileged `POST /api/v1/admin/catalog/shops/provision` writes now fail closed for refresh-only, forged-access, or expired protected sessions until the real `FT-007` refresh step restores access.
- `FT-010`: `TASK-FT010-09` closed the semantic runtime gap on admin provisioning, so `POST /api/v1/admin/catalog/shops/provision` now reuses the checked-in admin cookie/session family with RBAC/origin enforcement and no longer acts as an anonymous write surface.
- `FT-010`: `TASK-FT010-03` is complete; backend `catalog` now exposes an atomic admin provisioning command plus repo-local runtime path for shop bootstrap, Telegram-linked seller binding, and starter menu/product creation, and `TASK-FT010-04` is now `ready`.
- Drift correction: `REQ-004/005/006/021/022/023` больше не считаются закрытыми в текущем checked-in repo reality; RTM для них возвращен в `implemented` из-за stubbed/unmounted checkout runtime path, а seller contours зафиксированы как target contours, а не как уже смонтированный runtime.
- Seller catalog direction is now re-frozen around shared storefront edit mode, admin-provisioned skeleton shops, explicit `WORKING/NOT_WORKING` visibility, a narrow `seller-web` store-admin baseline, and a project-wide no-delete catalog policy; checked-in legacy soft-delete code is now tracked as spec/code drift.
- Telegram Mini App docs sync stabilized: временный research artifact удален, routing cleanup завершен, а новые `contracts/runbooks/diagrams` остались в роли нормативного и derived spec-layer.
- `FT-010`: implementation plan, protocol docs and execution-ready backlog decomposition are added for shared storefront seller edit mode, admin provisioning, Telegram-linked seller access and narrow `seller-web` status control.
- `FT-001`: contract layer extended with `catalog-public-api` and `seller-catalog-write-policy` for docs-first implementation.
- `FT-001`: backend `catalog` scaffold, Prisma baseline, and backend test skeleton added for `TASK-FT001-02`.
- `FT-001`: frontend `catalog` scaffold and public route shell added for `TASK-FT001-03`.
- `FT-001`: backend public browse read path for `shops/products` is implemented and verified via `TASK-FT001-04`.
- `FT-001`: seller-scoped shop writes and rename marker logic are implemented and verified via `TASK-FT001-05`.
- `FT-001`: seller-scoped product writes with shop linkage validation are implemented and verified via `TASK-FT001-06`.
- `FT-001`: frontend public catalog route now loads backend browse data and covers loading/empty/error states via `TASK-FT001-07`.
- `FT-001`: repo-local frontend route/page smoke coverage now verifies public catalog rendering for `TASK-FT001-07`.
- `FT-001`: final verification/docs sync completed in `TASK-FT001-08`, and RTM for `REQ-001/002/020` is now marked `done`.
- `FT-001`: repo-local backend Jest runner for catalog specs was added via `TASK-FT001-09` and now underpins backend verification.
- `FT-002`: implementation plan, protocol docs and execution-ready backlog decomposition were added for checkout/payment, Telegram auth and paid-only order creation.
- `FT-003`: implementation plan, protocol docs and execution-ready backlog decomposition were added for first-run language overlay, persistence fallback and post-auth language sync.
- `FT-002`: backend `checkout-payment` scaffold, Prisma order/payment baseline, and backend test skeleton were added and passed delegated verification via `TASK-FT002-02`.
- `FT-002`: frontend `checkout-payment` route shell, slice scaffold, and frontend smoke coverage were added and passed delegated verification via `TASK-FT002-03`.
- `FT-002`: backend Telegram auth validation, replay guard, and HttpOnly cookie session metadata were implemented and verified via `TASK-FT002-04`.
- `FT-002`: backend trusted payment finalization now verifies provider/source trust, creates paid orders idempotently, and is verified via `TASK-FT002-05`.
- `FT-002`: backend payment failure paths now return retry-safe controlled errors without order side effects and are verified via `TASK-FT002-06`.
- `FT-002`: frontend checkout route shell and repo-local UX smoke landed via `TASK-FT002-07`, but the checked-in app still defaults to a stubbed checkout API instead of a mounted backend checkout/auth path.
- `FT-002`: final repo-local verification/docs sync is complete via `TASK-FT002-08`, but later drift review reclassified `REQ-004/005/006/021/022/023` as `implemented` rather than closed until the real customer-facing checkout runtime is mounted.
- `FT-002`: app router now resolves the current pathname so `/checkout` renders the checkout flow instead of always falling back to catalog.
- `FT-002`: Telegram auth replay guard and Mini App session issuance now execute atomically, preventing duplicate sessions and stray `500` responses under concurrent retries.
- `FT-003`: docs-first language policy and verify ownership are frozen via `TASK-FT003-01`.
- `FT-003`: shared localization runtime scaffold now exists for language normalization, persistence orchestration, Telegram storage adapter ownership, and app-level overlay entrypoints via `TASK-FT003-02`.
- `FT-003`: deterministic language resolution, fail-safe storage fallback, and Telegram storage adapter wrappers are implemented and verified via `TASK-FT003-03`.
- `FT-003`: first-run language overlay now fully gates customer-facing routes, and checkout auth synchronizes explicit language choice into backend profile state via `TASK-FT003-04`.
- `FT-003`: customer-facing overlay, catalog, and checkout baseline copy now render in the selected language and are repo-locally verified via `TASK-FT003-05`.
- `FT-003`: final repo-local localization verification is complete via `TASK-FT003-06`; shell-owned evidence for shared `REQ-022/023` exists in `FT-009`, but the shared RTM rows remain `implemented` until checkout runtime drift in `FT-002` is removed.
- `FT-004`: implementation plan, protocol docs and execution-ready backlog decomposition were added for courier assignment, `CREATED -> ASSIGNED` ownership, `order.assigned` semantics and targeted courier notification.
- `FT-004`: docs-first assignment boundary, `order.assigned` event semantics, and courier-targeted notification policy are frozen via `TASK-FT004-01`.
- `FT-004`: backend `delivery-assignment` scaffold, Prisma persistence baseline, and backend test harness are added and verified via `TASK-FT004-02`.
- `FT-004`: separate `admin-web` assignment route shell and frontend smoke harness are now in place via `TASK-FT004-03` without pulling `FT-007` login/session scope into the slice.
- `FT-004`: backend assignment command now enforces auth/RBAC plus `CREATED -> ASSIGNED`, writes order history/audit, publishes canonical `order.assigned`, and keeps invalid requests side-effect free via `TASK-FT004-04`.
- `FT-004`: targeted courier bot notification now dispatches only to the assigned courier and keeps transport failures duplicate-safe relative to assignment side effects via `TASK-FT004-05`.
- `FT-004`: admin-web assignment submit flow now calls the backend command path, surfaces controlled success/error feedback, and blocks duplicate submit side effects via `TASK-FT004-06` while keeping `FT-007` auth/session ownership out of scope.
- `FT-004`: final repo-local verification/docs sync is complete via `TASK-FT004-07`; `REQ-007` and the `FT-004` `REQ-018` trace row are now `done`, while post-assignment lifecycle and admin auth/session ownership remain with `FT-005` and `FT-007`.
- `FT-005`: implementation plan, protocol docs and execution-ready backlog decomposition were added for delivery tracking, ordered event polling, `409 CONFLICT` state-machine enforcement and SLA verification.
- `FT-005`: docs-first state-machine, polling cursor contract, `409 CONFLICT`, and SLA verification ownership are frozen via `TASK-FT005-01`, unlocking backend/frontend scaffold tasks.
- `FT-005`: backend `delivery-tracking` scaffold, slice-owned persistence wiring, and backend test baseline are added via `TASK-FT005-02`, unlocking the courier status command task while keeping RTM rows planned until runtime behavior lands.
- `FT-005`: frontend `order-tracking` polling-consumer scaffold and transport-only courier bot harness are added via `TASK-FT005-03`, keeping runtime adapters out of state-machine ownership and leaving `REQ-009/010` open for later runtime/SLA tasks.
- `FT-005`: backend courier status command now enforces authenticated courier ownership plus adjacent transitions, writes `order_status_history` and `order.status_changed`, and returns polling-friendly `updatedAt`/`revision` via `TASK-FT005-04`, unlocking ordered polling work in `TASK-FT005-05` while RTM rows remain open pending later runtime closure.
- `FT-005`: backend ordered polling now returns stable event objects with string `revision` / `nextCursor`, preserves ascending event order, and keeps duplicate polling read-side effect free via `TASK-FT005-05`, unlocking notification/runtime wiring in `TASK-FT005-06` while RTM rows remain open pending later feature/SLA closure.
- `FT-005`: committed status changes now fan into Telegram courier notifications via slice-owned notifier wiring, while the frontend polling consumer dedupes command-confirmed revisions across interval retries/resume and keeps action labels aligned with ordered event state via `TASK-FT005-06`; `TASK-FT005-07` is now ready and RTM rows stay open pending final end-to-end closure.
- `FT-005`: final repo-local functional verification is complete via `TASK-FT005-07`; backend/frontend tests now cover the courier path to `COMPLETED`, ordered polling observation, and `409 CONFLICT`/history/event evidence, so `REQ-008`, `REQ-009`, and the `FT-005` `REQ-018` row are `done`, while `REQ-010` remains with `TASK-FT005-08`.
- `FT-005`: final SLA closure is complete via `TASK-FT005-08`; the new repo-local polling harness measured `p95 = 4500 ms` and `max = 4750 ms` across the current 5-second polling cadence, so `REQ-010` is now `done` and `FT-005` is fully closed.
- `FT-006`: implementation plan, protocol docs and execution-ready backlog decomposition were added for operational cancellation, allowed-role policy, manual refund tracking and final refund evidence sync.
- `FT-006`: docs-first cancellation policy, refund-state semantics (`NOT_REQUIRED/PENDING_MANUAL/DONE/REJECTED`), and verify boundary are now frozen via `TASK-FT006-01`.
- `FT-006`: backend `order-cancellation` scaffold, slice-owned cancellation/refund persistence baseline, and repo-local Jest harness are now in place via `TASK-FT006-02`, unlocking `TASK-FT006-04` while RTM rows stay open pending runtime behavior and final evidence.
- `FT-006`: admin-web cancellation/refund route shell, explicit refund-state placeholder rendering, and repo-local admin frontend smoke coverage are now in place via `TASK-FT006-03` without pulling `FT-007` auth/session or review flow scope into the slice.
- `FT-006`: backend cancellation command now enforces allowed-role/state policy, assigned-courier unavailable-case rules, and transactional order/history/audit/event writes via `TASK-FT006-04`; `TASK-FT006-05` is now unblocked while RTM rows stay `planned` until refund progression, UI wiring, and final verify evidence land.
- `FT-006`: backend manual refund progression now enforces cancelled paid-order `PENDING_MANUAL -> DONE/REJECTED`, persists required operator notes, and publishes audit/event artifacts via `TASK-FT006-05`; `TASK-FT006-06` is now ready while RTM rows remain open pending UI wiring and final verify evidence.
- `FT-006`: admin-web cancellation/refund route now calls explicit backend command paths, renders controlled forbidden/error outcomes, keeps refund-state/note visibility after submit, and blocks duplicate submit side effects via `TASK-FT006-06`; `TASK-FT006-07` is now ready while RTM rows remain open pending final verification evidence.
- `FT-006`: final repo-local verification suite now covers allowed admin/courier cancellation outcomes, client prohibition, actor/reason persistence, explicit refund-state visibility, and cancellation/refund audit-event evidence via `TASK-FT006-07`; `REQ-011` is now `done`, `TASK-FT006-08` is `ready`, and refund/runbook closure remains with the final evidence-sync task.
- `FT-006`: final refund runbook evidence and docs closure are complete via `TASK-FT006-08`; manual refund workflow confirmation is now explicit in the runbook/spec layer, and `REQ-012` plus the `FT-006` `REQ-018` RTM row are `done`.
- `FT-007`: implementation plan, protocol docs and execution-ready backlog decomposition were added for admin auth, lockout policy, session lifetime enforcement and auth audit closure.
- `FT-007`: docs-first auth freeze is complete via `TASK-FT007-01`; MVP now explicitly fixes boss-controlled out-of-band provisioning and HTTPS-only HttpOnly cookie transport for admin sessions.
- `FT-007`: backend `admin-access` scaffold, Prisma credentials/session/audit baseline, and repo-local unit/integration harness are now in place via `TASK-FT007-02`, unlocking backend login/runtime work in `TASK-FT007-04` without moving auth invariants into `shared`.
- `FT-007`: admin-web login route, shared protected shell, and frontend smoke harness are now in place via `TASK-FT007-03`, keeping auth policy centralized and leaving runtime login/refresh/logout behavior to later backend/frontend tasks.
- `FT-007`: backend login now authenticates provisioned admins, writes `login_success/login_failed/locked` audit events, creates hashed refresh-session baseline records, and returns controlled `401/429` outcomes via `TASK-FT007-04`; refresh/logout lifetime enforcement is now the next unlocked step.
- `FT-007`: refresh rotation, logout revocation, idle-timeout expiry, and fixed 3-day session lifetime are now implemented in `admin-access` via `TASK-FT007-05`; lockout also revokes active session chains, and `TASK-FT007-06` is now `ready` for admin-web UX wiring.
- `FT-007`: admin-web now uses one shared cookie-based auth boundary for login, refresh restore, protected-route fallback, and logout across assignment/cancellation pages via `TASK-FT007-06`.
- `FT-007`: final repo-local verification/docs sync is complete via `TASK-FT007-07`; `REQ-015`, `REQ-016`, `REQ-017`, and the `FT-007` `REQ-018` row are now `done`.
- `FT-008`: implementation plan, protocol docs and execution-ready backlog decomposition were added for two-sided bot reviews, duplicate-safe review submission and negative alert escalation.
- `FT-008`: docs-first review payload boundary, `COMPLETED` activation gate, duplicate-safe negative alert semantics, and verify ownership are now frozen via `TASK-FT008-01`.
- `FT-008`: backend `reviews-feedback` scaffold, Prisma `Review` baseline, and repo-local Jest harness are now in place via `TASK-FT008-02`, unlocking backend review command work in `TASK-FT008-04` while RTM rows remain open pending runtime behavior and final verification.
- `FT-008`: transport-only Telegram review prompts, callback parsing, and unique-target negative alert harness are now in place via `TASK-FT008-03`, leaving completed-only review submission and canonical `review.negative` publication to later runtime tasks.
- `FT-008`: completed-only review submission now validates actor/direction ownership, persists structured review payloads, and keeps duplicate bot replay side-effect free via `TASK-FT008-04`, so `TASK-FT008-05` is now ready while `REQ-013` / `REQ-014` remain open pending low-rating publication, bot flow wiring, and final verification.
- `FT-008`: low-rating reviews now publish canonical `review.negative`, resolve active admin Telegram targets inside the owning slice boundary, and fan out duplicate-safe alerts via `TASK-FT008-05`; `TASK-FT008-06` is now ready while RTM rows remain open pending bot-guided flow wiring and final verification.
- `FT-008`: bot-guided client and courier review runtime is now wired via `TASK-FT008-06`, so both sides advance through `rating -> reason_code -> comment(optional)` and submit through the owning backend path; `TASK-FT008-07` is now ready for final verification/docs/RTM closure.
- `FT-008`: final repo-local verification/docs sync is complete via `TASK-FT008-07`; two-sided bot review evidence, courier/client low-rating alert fan-out, and RTM closure for `REQ-013/014` are now done.
- `FT-008`: stale Telegram review callbacks are now rejected with revision-aware step validation via `TASK-FT008-08`, keeping intermediate bot-step replay side-effect free without changing final-submit idempotency.
- `FT-008`: adversarial verification on `TASK-FT008-08` correctly identified draft durability as the remaining follow-up, and that runtime gap is now closed by `TASK-FT008-09`.
- `FT-008`: review drafts now persist in slice-owned durable storage with explicit `1 hour` TTL via `TASK-FT008-09`, so restart/redeploy and shared-DB multi-instance hops no longer rely on implicit in-memory state.
- `FT-008`: `red-verify` for `TASK-FT008-09` did not find a semantic break in the review flow, but left a follow-up `TASK-FT008-10` for checked-in Prisma rollout/retention closure before calling the durability path fully risk-closed.
- `FT-008`: `TASK-FT008-10` added checked-in Prisma rollout artifacts for `ReviewDraft` and explicit expired-draft cleanup policy, so the durability path is now operationally deployable instead of relying on implicit rollout/retention assumptions.
- Semantic review after PR `#6` opened three follow-ups: `TASK-FT007-08` for the missing real admin HTTP cookie auth boundary, `TASK-FT008-08` for stale Telegram review callback hardening, and `TASK-FT008-09` for making review-draft runtime guarantees explicit instead of implicit.
- `FT-007`: `TASK-FT007-08` added the admin HTTP cookie auth boundary and transport enforcement, but `red-verify` found that the handler was still test-mounted only.
- `FT-007`: `TASK-FT007-09` mounted that handler into the checked-in repo-local backend runtime used by `dev:api` and the Vite `/api` proxy, so local/dev login/refresh/logout now hit the real mounted runtime instead of a test-only server shell.
- `FT-007`: `TASK-FT007-10` fixed the remaining deployed frontend bootstrap gap, so `/admin/*` paths now select the admin contour through the shared entrypoint instead of falling back to the customer catalog.
- Ops: added a containerized deploy path for the same `tgmeal.natureonzoom.win` VPS, with checked-in `Dockerfile.web`, `Dockerfile.api`, `docker-compose.yml`, and a dedicated runbook for replacing the older non-container app copy.
- Ops: deployment specs are now split explicitly into architecture (`deployment-and-runtime-topology`), practical guide (`server-deploy-and-rollout`), and canonical VPS runbook (`telegram-mini-app-container-deploy`), so server rollout is documented as a first-class project concern rather than scattered notes.
- `FT-009`: implementation plan, protocol docs and execution-ready backlog decomposition were added for Telegram Mini App shell/runtime baseline, WebView UX hardening and final client-matrix verification.
- `FT-009`: current verification policy treats operator-confirmed real `Android Telegram` notes as advisory pre-release risk evidence; screenshots/videos are optional hardening only.
- Added a dedicated VPS + Cloudflare + Telegram test-server runbook for first real Android Mini App launches on `tgmeal.natureonzoom.win`.
- `FT-009`: docs-first shell/runtime ownership, shared storage boundary, and verification routing were frozen via `TASK-FT009-01`, unlocking app-level shell scaffolding in `TASK-FT009-02`.
- `FT-009`: app-level shell boundary, shared shell state/context scaffold, and runtime bridge test harness are now in place via `TASK-FT009-02`, unlocking runtime event implementation in `TASK-FT009-03`.
- `FT-009`: runtime adapter wiring for `ready()/expand()`, theme, safe-area, stable viewport, lifecycle, and shell CSS variable propagation is complete via `TASK-FT009-03`, unlocking slice-level UX integration in `TASK-FT009-04`.
- `FT-009`: baseline shell/runtime closure was previously reached via `TASK-FT009-06`; later hardening follow-up tasks `TASK-FT009-07` and `TASK-FT009-08` are now checked in, with `/verify` passing for both, but the feature is not yet risk-closed because their post-change `red-verify` results leave the final keyboard-open/degradation-policy closure to `TASK-FT009-09`.

## Current MVP map

- `EP-001`: стартовая Витрина, public catalog, customer cart/order composition, checkout/payment, customer status visibility, shared seller storefront editing and обязательная локализация.
- `EP-002`: операционный delivery flow: assignment, tracking, polling, cancellation, refund tracking.
- `EP-003`: отдельный security/auth контур веб-админки.
- `EP-004`: post-delivery feedback loop и negative alerts.
